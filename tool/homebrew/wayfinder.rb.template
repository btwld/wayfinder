# Generated by btwld/wayfinder release tooling.
class Wayfinder < Formula
  desc "Local knowledge validation, semantic search and MCP server"
  homepage "https://github.com/btwld/wayfinder"
  version "{{VERSION}}"
  license all_of: ["BSD-3-Clause", "Apache-2.0", "MIT"]

  on_macos do
    depends_on arch: :arm64
    url "https://github.com/btwld/wayfinder/releases/download/wayfinder-v{{VERSION}}/wayfinder-macos-arm64.tar.gz"
    sha256 "{{MACOS_SHA256}}"
  end
  on_linux do
    depends_on arch: :x86_64
    url "https://github.com/btwld/wayfinder/releases/download/wayfinder-v{{VERSION}}/wayfinder-linux-x64.tar.gz"
    sha256 "{{LINUX_SHA256}}"
  end

  def install
    libexec.install Dir["*"]
    bin.install_symlink libexec/"bin/wayfinder"
  end

  test do
    assert_match "wayfinder #{version}", shell_output("#{bin}/wayfinder --version")
    assert_path_exists libexec/"models/embedding.gguf"
    assert_path_exists libexec/"licenses/objectbox/NOTICE"
  end
end

# skills
The skill set the agent uses (grill, to-spec, to-tickets, implement, tdd, code-review), adapted from the Matt Pocock model. Shared skills can be installed and versioned via the Stargate Workspace Registry (RFC #224).

name: station
description: Local OKF validation, knowledge indexing and semantic search.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.7

executables:
  station:

dependencies:
  ack: ^1.2.0
  ack_mcp_dart: ^1.3.0
  args: ^2.7.0
  crypto: ^3.0.3
  knowledge_embeddings: ^0.1.0
  mcp_dart: ^2.4.2
  okf_profile: ^0.2.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
